# Firebase Setup

This project currently uses hardcoded values in the HTML files.

Firebase setup artifacts now live in:

- [firebase/firestore-schema.md](firebase/firestore-schema.md)
- [firebase/firestore.rules](firebase/firestore.rules)
- [firebase/README.md](firebase/README.md)
- [scripts/migrate-firestore-data.js](scripts/migrate-firestore-data.js)

The migration script is intentionally safe by default and will not write any data unless you explicitly enable it.

